#[databases]
# Configura la base de datos que PgBouncer gestionará, redirigiéndola a la instancia de PostgreSQL
#mi_base_de_datos = host=postgres port=5432 dbname=mi_base_de_datos user=mi_usuario password=mi_contrasena_segura

#[pgbouncer]
#listen_addr = 0.0.0.0
#listen_port = 6432
#auth_type = md5
#auth_file = /etc/pgbouncer/userlist.txt
#pool_mode = session
#max_client_conn = 100
#default_pool_size = 20

################## Auto generated ##################
[databases]
mi_base_de_datos = host=postgres port=5432 auth_user=mi_usuario

[pgbouncer]
listen_addr = 0.0.0.0
listen_port = 6432
unix_socket_dir = 
user = postgres
auth_file = /etc/pgbouncer/userlist.txt
auth_type = scram-sha-256
pool_mode = session
max_client_conn = 100
default_pool_size = 20
ignore_startup_parameters = extra_float_digits

# Log settings
admin_users = postgres

# Connection sanity checks, timeouts

# TLS settings

# Dangerous timeouts
################## end file ##################
